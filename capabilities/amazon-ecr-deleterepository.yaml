naftiko: 1.0.0-alpha2
info:
  label: 'Amazon ECR Amazon Elastic Container Registry (ECR) API — #DeleteRepository'
  description: 'Amazon ECR Amazon Elastic Container Registry (ECR) API — #DeleteRepository. 1 operations. Lead operation:
    Amazon ECR Delete Repository. Self-contained Naftiko capability covering one Amazon Ecr business surface.'
  tags:
  - Amazon Ecr
  - '#DeleteRepository'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_ECR_API_KEY: AMAZON_ECR_API_KEY
capability:
  consumes:
  - type: http
    namespace: amazon-ecr-deleterepository
    baseUri: https://api.ecr.amazonaws.com
    description: 'Amazon ECR Amazon Elastic Container Registry (ECR) API — #DeleteRepository business capability. Self-contained,
      no shared references.'
    resources:
    - name: '#DeleteRepository'
      path: /#DeleteRepository
      operations:
      - name: deleterepository
        method: POST
        description: Amazon ECR Delete Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: X-Amz-Target
          in: header
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.AMAZON_ECR_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: amazon-ecr-deleterepository-rest
    port: 8080
    description: 'REST adapter for Amazon ECR Amazon Elastic Container Registry (ECR) API — #DeleteRepository. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/deleterepository
      name: deleterepository
      description: 'REST surface for #DeleteRepository.'
      operations:
      - method: POST
        name: deleterepository
        description: Amazon ECR Delete Repository
        call: amazon-ecr-deleterepository.deleterepository
        with:
          X-Amz-Target: rest.X-Amz-Target
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amazon-ecr-deleterepository-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for Amazon ECR Amazon Elastic Container Registry (ECR) API — #DeleteRepository. One tool per
      consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: amazon-ecr-delete-repository
      description: Amazon ECR Delete Repository
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: amazon-ecr-deleterepository.deleterepository
      with:
        X-Amz-Target: tools.X-Amz-Target
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
